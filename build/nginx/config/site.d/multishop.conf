map $http_host $mage_code {
hostnames;
        .openmage.test default;
        .de.openmage.test de;
}

map $http_host $mage_type {
hostnames;
        .openmage.test store;
        .de.openmage.test store;
}